# 预发布/测试环境配置
# Staging/Testing Environment Configuration

# 远程测试 Supabase 实例
VITE_SUPABASE_URL=https://your-staging-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-staging-anon-key-here

# 环境标识
VITE_APP_ENV=staging

# 后端 API 配置
VITE_API_URL=https://your-staging-api.com

# 应用配置
VITE_APP_NAME=发票助手 (测试)
VITE_APP_VERSION=2.0.0-staging

# 调试配置
VITE_DEBUG_MODE=true

# 注意：
# 1. 此环境用于预发布测试和质量保证
# 2. 可以包含测试数据和调试功能
# 3. 建议使用独立的 Supabase 项目避免影响生产数据