# Supabase 配置
VITE_SUPABASE_URL=your-supabase-project-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# 后端API配置
VITE_API_URL=http://localhost:8070

# 应用配置
VITE_APP_NAME=发票助手
VITE_APP_VERSION=2.0.0

# 注意：不要在 .env 文件中设置 NODE_ENV=production
# Vite 只支持 NODE_ENV=development 在 .env 文件中
# 生产环境请在启动命令中设置：NODE_ENV=production npm run build
EOF < /dev/null